# ============================================================
# requirements.txt — Backend FastAPI
# Smart Insect Identifier
# ============================================================

# Web Framework
fastapi==0.111.1
uvicorn[standard]==0.30.1

# ML Inference (CPU mode untuk backend lokal)
# PyTorch CPU-only — lebih ringan, tidak butuh CUDA di backend
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu
torchvision==0.18.1+cpu

# Image Processing
Pillow==10.3.0

# Google Gemini SDK
google-generativeai==0.7.2

# Environment Variables
python-dotenv==1.0.1

# Data validation (sudah include di FastAPI, tapi eksplisit lebih aman)
pydantic==2.7.4

# Logging & Utilities
python-multipart==0.0.9   # Wajib untuk UploadFile di FastAPI
